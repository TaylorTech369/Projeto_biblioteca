import { StatusBar } from 'expo-status-bar';
import { StyleSheet, Text, View, SafeAreaView } from 'react-native';

export default function App() {
  return (
    <SafeAreaView style={styles.container}>
      <StatusBar style="dark" />

      {/* Capa do Livro (Fundo levemente maior) */}
      <View style={styles.capa_livro}>
        
        {/* Lombada/Sombra interna para parecer a dobra do livro */}
        <View style={styles.lombada_esquerda} />

        <View style={styles.pagina_conteudo}>
          <Text style={styles.boas_vindas}>
            📚 Projeto Biblioteca
          </Text>
          
          <View style={styles.divisor_elegante} />
          
          <Text style={styles.corpo_texto}>
            "A leitura é uma fonte inesgotável de prazer mas por incrível que pareça, a quase totalidade, não serve para nada."
          </Text>
        </View>

      </View>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    // Cor de fundo: Carvalho bem claro/Areia
    backgroundColor: '#F2E8D5', 
    alignItems: 'center',
    justifyContent: 'center',
  },

  capa_livro: {
    backgroundColor: '#D7BC95', // Tom de couro claro ou madeira polida
    width: '92%',
    height: '85%',
    borderRadius: 4,
    flexDirection: 'row',
    // Sombra para o livro "flutuar" na mesa
    elevation: 20,
    shadowColor: '#000',
    shadowOffset: { width: 10, height: 10 },
    shadowOpacity: 0.2,
    shadowRadius: 15,
  },

  lombada_esquerda: {
    width: '8%',
    height: '100%',
    backgroundColor: 'rgba(0,0,0,0.15)', // Sombra que simula a dobra do livro
    borderRightWidth: 1,
    borderRightColor: 'rgba(0,0,0,0.05)',
  },

  pagina_conteudo: {
    flex: 1,
    backgroundColor: '#FFFBF2', // Cor de papel marfim (calma e clara)
    margin: 5,
    marginLeft: 0, // Encosta na lombada
    padding: 25,
    borderTopRightRadius: 5,
    borderBottomRightRadius: 5,
    alignItems: 'center',
  },

  boas_vindas: {
    fontSize: 22,
    fontWeight: '600',
    color: '#5D4037',
    textAlign: 'center',
    marginTop: 20,
    letterSpacing: 0.5,
  },

  divisor_elegante: {
    width: '40%',
    height: 1,
    backgroundColor: '#D7BC95',
    marginVertical: 25,
  },

  corpo_texto: {
    fontSize: 16,
    color: '#8D6E63',
    fontStyle: 'italic',
    textAlign: 'center',
    lineHeight: 24,
  }
});